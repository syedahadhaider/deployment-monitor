{
  "name": "deployment-monitor-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply deployment-monitor --local",
    "migrate": "wrangler d1 migrations apply deployment-monitor --remote",
    "verify-sql": "python verify_sql.py"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.0.0"
  }
}
